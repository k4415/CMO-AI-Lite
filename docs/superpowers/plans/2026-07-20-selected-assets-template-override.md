# 選択素材テンプレ優先例外 実装計画

> 対象: `/Users/koukamiyoshihiko/CMO-AI-Lite-main`
>
> 方針: ユーザー選択素材だけを閉じたテンプレ構造の唯一の例外にする。未選択素材や任意の新規視覚要素は追加しない。

## Task 1: 仕様を失敗テストで固定する

対象:

- `tests/banner-template-structure.test.js`
- `tests/banner-logo-reference.test.js`

手順:

1. 画像スロット0件、役割不一致、容量超過でも選択素材が許可されるテストを追加する。
2. 無選択時は例外が無効であるテストを追加する。
3. 通常画像プロンプトと再生成プロンプトの両方に、選択素材必須・未選択素材禁止が入るテストを追加する。
4. 対象テストを実行し、現実装でREDになることを確認する。

## Task 2: 構造契約へ選択素材例外を実装する

対象:

- `src/core/banner-template-structure.js`
- `src/core/banner-ai.js`
- `config/prompts/banner.md`

手順:

1. 保存済み選択パスから `selectedAssetPolicy` を決定論的に構築する。
2. 選択素材のスロット有無・役割・容量を理由にthrowしないよう変更する。
3. 閉じた構造の指示に「選択素材だけが唯一の例外」を追加する。
4. Stage 2プロンプトと正規化済み `promptJson` に同一ポリシーを反映する。
5. Task 1のテストをGREENにする。

## Task 3: 画像生成と再生成へ同じ例外を実装する

対象:

- `src/core/openai-image.js`

手順:

1. 実際に添付された入力画像を選択済み必須素材として扱う。
2. ロゴ・商品・その他素材の全役割で、使用必須と未選択素材禁止を明示する。
3. 対応スロットがない場合も、選択素材だけを最小限配置する指示へ変更する。
4. 通常生成と再生成の両プロンプトへ適用する。
5. Task 1のテストをGREENにする。

## Task 4: 文書と回帰検証を完了する

対象:

- `docs/data-model.md`
- 必要に応じた既存テスト

手順:

1. `selectedAssetPolicy` と優先順位をデータモデルへ記載する。
2. 対象テスト、全体テスト、構文確認、`git diff --check` を実行する。
3. 実案件 `oh-my-teeth-20260720` の失敗バナーを読み取り、純粋なプロンプト生成経路で構造エラーが消えることを確認する。
4. 有料の画像生成APIは検証では呼ばない。
5. 実装セルフレビューを行い、8.5/10未満なら修正と再検証を繰り返す。
6. 外部レビューCLIは1回だけ試し、認証不可ならスキップ理由を記録する。

## 完了条件

- 受け入れ条件をテストで証明できる。
- 実案件の既存失敗入力で `TEMPLATE_IMAGE_ROLE_MISMATCH` が再発しない。
- 未選択時の厳格な閉じた構造を維持できる。
- コミットはユーザー指示がないため行わない。
